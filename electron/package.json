{
  "name": "couchdb-client",
  "version": "1.0.0",
  "description": "A minimal CouchDB client for managing databases and documents",
  "main": "main.js",
  "author": "Hasan Abbas",
  "license": "MIT",
  "scripts": {
    "electron": "electron .",
    "electron:dev": "NODE_ENV=development electron .",
    "prepare:frontend": "node scripts/prepare-frontend.js",
    "build:mac": "yarn prepare:frontend && electron-builder --mac",
    "build:win": "yarn prepare:frontend && electron-builder --win --x64",
    "build:all": "yarn prepare:frontend && electron-builder --mac --win --x64"
  },
  "devDependencies": {
    "electron": "^28.1.3",
    "electron-builder": "^24.9.1"
  },
  "build": {
    "appId": "com.couchdb.client",
    "productName": "CouchDB Client",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "package.json",
      "frontend-build/**/*"
    ],
    "mac": {
      "category": "public.app-category.developer-tools",
      "target": ["dmg", "zip"],
      "icon": "icon.png"
    },
    "win": {
      "target": ["nsis", "portable"],
      "icon": "icon.png"
    },
    "linux": {
      "target": ["AppImage", "deb"],
      "category": "Development"
    }
  }
}
